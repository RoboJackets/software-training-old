#include "STSL/RJRobot.h"


class RJRobotExtended : public RJRobot {
//RJRobot Extended has 2 public methods
// A constructor that takes in a RobotType and a std::chrono::milliseconds
// A method called DriveInSquare that drives the robot in a square
public:

//RJRobotExtended has 1 private variable, a std::chrono::milliseconds called duration
//RJRobotExtended has 2 private methods
// A method called DriveEdge which drives the edge of the square for `duration` milliseconds
// A method called DriveCorner which turns the robot 90 degrees to the right
private:


};